# Render-hosted websocket backend (service: srv-d7ucfptckfvc73espc7g).
# Vite inlines this at build time so the static Netlify deploy can reach
# the realtime gateway over wss.
VITE_WS_URL=wss://hunted-horror-game2.onrender.com/ws
